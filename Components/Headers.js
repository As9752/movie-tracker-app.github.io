import  "./Headers.css"
const Headers = () => {
    return (
        <div>
            <span onClick={()=>window.scroll(0,0)}className="header">Movie Tracker App</span>
        </div>
    )
}

export default Headers

